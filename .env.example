DATABASE_URL="file:./dev.db"

# Optional: enable real AI generation with Anthropic's Claude API.
# Without this key the app falls back to a built-in template engine,
# so it works out of the box with no external dependency.
ANTHROPIC_API_KEY=
