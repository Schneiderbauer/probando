DATABASE_URL="file:./dev.db"

# REQUIRED for script generation: real Claude API key.
# The library (feed, ingesta de referencias) funciona sin esta clave, pero
# generar guiones requiere una conexión real a la API de Anthropic — sin
# clave, la app muestra un aviso claro en vez de generar contenido genérico.
ANTHROPIC_API_KEY=
